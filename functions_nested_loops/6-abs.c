#include "main.h"
#include <stdio.h>

/**
 * _abs - Entry point
 * @n: Variable
 *
 * Description: Checks if lowercase
 *
 * Return: Always 0 (Success)
 */

int _abs(int n)
{
	if (n >= 0)
	{
		return (n);
	}
	else
	{
		return (-n);
	}
}
